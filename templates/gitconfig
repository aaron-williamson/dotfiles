[diff]
  tool = vimdiff
  {{GIT_SUBMODULE}}
[difftool]
  prompt = false
[merge]
  tool = vimdiff
  conflictstyle = diff3
[mergetool]
  prompt = false
[user]
  email = {{GIT_EMAIL}}
  name = {{GIT_NAME}}
[alias]
  dt = difftool
  lg = log --graph --oneline
  st = status
  aa = add --all
  ap = add -p
  amend = commit --amend
  di = diff
  dc = diff --cached
  co = checkout
  ci = commit --verbose
  fa = fetch --all
  lp = !git lg -p
  rt = rev-parse --show-toplevel
  sb = show-branch --list
  sbo = ls-remote -h origin
  sbu = ls-remote -h upstream
  sbr = !git sbo
  lb = branch --list
  lbr = branch -r --list
  lba = branch -a
  sm = submodule
[status]
  submodulesummary = 1
[color]
  ui = auto
{{GIT_PUSH}}
